# Starter Prompts — Full-Stack Indie Developer

## MVP Launch
- Scaffold a SaaS landing page: hero + features + pricing + Stripe checkout
- Design the database schema for [your idea] — keep it minimal, PostgreSQL only
- Set up Supabase auth with magic link + Google OAuth + RLS policies
- Create a dashboard page with real-time data from Supabase

## Deployment
- Deploy this Next.js app to Cloudflare Pages with custom domain
- Set up a Hono API on Cloudflare Workers with D1 database
- Configure GitHub Actions CI/CD: lint + build + deploy on push to main
- Add Plausible analytics — self-hosted or cloud, zero cookie banner

## Speed Hacks
- Convert this component to use server components (reduce client JS)
- Add ISR (Incremental Static Regeneration) to this page for faster loads
- Set up edge caching with Cloudflare for API responses
- Profile and fix the slowest page — target LCP < 2s

## Payments & Growth
- Integrate Stripe Checkout with subscription plans (monthly + annual)
- Add a referral system: unique invite links + reward tracking
- Build an email capture form with Resend for transactional emails
- Create a changelog page that auto-generates from git commits

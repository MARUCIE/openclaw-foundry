# Starter Prompts — Frontend Engineer

## Component Development
- Build an accessible dropdown menu with keyboard nav (Tab, Arrow, Escape, Enter)
- Create a reusable Modal component with focus trap and Escape to close
- Build a data table with sorting, filtering, pagination, and sticky header
- Create a toast/notification system with auto-dismiss and stack management

## Performance
- Audit this page for Core Web Vitals — fix LCP, CLS, and FID issues
- Analyze the bundle size and identify the top 5 largest dependencies
- Convert this client component to a server component to reduce JS
- Add lazy loading for below-the-fold images and components

## Responsive & Design
- Create a responsive grid layout that works from 375px to 4K
- Build a mobile navigation with hamburger menu and slide-in drawer
- Implement a dark mode toggle using CSS custom properties
- Make this form accessible: labels, error messages, focus management

## Animation
- Add smooth scroll-triggered animations using CSS transitions
- Build a page transition with exit/enter animations
- Create a skeleton loading state that matches the layout
- Add micro-interactions: button press, card hover lift, input focus glow

## Testing
- Write Playwright E2E tests for the login flow (happy + error paths)
- Add visual regression tests for the 3 most important pages
- Test this component with screen reader (VoiceOver) and fix issues
- Check keyboard navigation through the entire page tab order
